[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "cash-flow-domain"
version = "0.1.0"
description = "Shared domain layer for the cash flow prediction platform."
requires-python = ">=3.13"
dependencies = []

[tool.uv]
package = true

[tool.hatch.build.targets.wheel]
packages = ["src/domain"]
